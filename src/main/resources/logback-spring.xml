<?xml version="1.0" encoding="UTF-8"?>
<!--
    Logs estruturados.
    - dev: console texto, leg�vel
    - prod: console JSON (Logstash encoder) com campos padronizados
            (timestamp, level, logger, message, mdc.*, stack_trace)

    NUNCA logar passwords, tokens completos, CPFs ou outros dados sens�veis.
    Filtros de mensagem rejeitam linhas contendo essas palavras-chave por seguran�a.
-->
<configuration>

    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="spectrum-ai-backend"/>

    <!-- Filtro de seguran�a: descarta logs com padr�es de dados sens�veis -->
    <turboFilter class="ch.qos.logback.classic.turbo.MarkerFilter">
        <Marker>SENSITIVE</Marker>
        <OnMatch>DENY</OnMatch>
    </turboFilter>

    <springProfile name="dev">
        <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="STDOUT"/>
        </root>
    </springProfile>

    <springProfile name="prod">
        <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeContext>false</includeContext>
                <includeMdc>true</includeMdc>
                <customFields>{"app":"${appName}"}</customFields>
                <fieldNames>
                    <timestamp>timestamp</timestamp>
                    <message>message</message>
                    <logger>logger</logger>
                    <thread>thread</thread>
                    <level>level</level>
                    <levelValue>[ignore]</levelValue>
                    <version>[ignore]</version>
                </fieldNames>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON"/>
        </root>
        <logger name="com.spectrumai" level="INFO"/>
        <logger name="org.hibernate.SQL" level="WARN"/>
        <logger name="org.springframework.security" level="WARN"/>
    </springProfile>

</configuration>
